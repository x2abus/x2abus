FROM python:3.11-slim
WORKDIR /app
COPY backend /app/backend
RUN pip install -U pip && pip install -r backend/requirements.txt
ENV HOST=0.0.0.0 PORT=8010
EXPOSE 8010
CMD ["python", "-m", "uvicorn", "backend.server:app", "--host", "0.0.0.0", "--port", "8010"]